/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["https://media.licdn.com/","https://encrypted-tbn0.gstatic.com/","wallpapers.com"]
    },
};

export default nextConfig;
